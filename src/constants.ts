/**
 * Default starting page number for pagination
 */
export const DEFAULT_PAGE = 1;

/**
 * Default number of items to display per page
 */
export const DEFAULT_LIMIT = 10;

/**
 * Default sort column (none)
 */
export const DEFAULT_SORT = null;

/**
 * Default sort order (none)
 */
export const DEFAULT_ORDER = null;

/**
 * Default empty filter object
 */
export const DEFAULT_FILTER = {};

/**
 * Ascending sort direction identifier
 */
export const SORT_ASC = "asc";

/**
 * Descending sort direction identifier
 */
export const SORT_DESC = "desc";

/**
 * Flag to determine if page should reset when query parameters change
 */
export const RESET_PAGE_ON_QUERY_CHANGE = true;
